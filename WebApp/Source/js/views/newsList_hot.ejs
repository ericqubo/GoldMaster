<%
    if(list != null && list.length > 0) {
        for(var i = 0;i < list.length; i++) {
        var item = list[i];
     %>
<li data-forhash="#newsDetail/<%= item.ID %>" data-transition="slideLeft">
    <p class="thelistp"><%= item.Title %></p>
    <span>
        <img src="<%= item.ImgUrl %>" style="width: 320px; height: 163px;">
    </span>
</li>
<%} }%>